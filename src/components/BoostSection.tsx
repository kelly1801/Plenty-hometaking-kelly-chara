import React from "react";
import { SectionContainer, TextContainer, Button } from "../styles";
const BoostSection = () => {
  return (
    <SectionContainer boost>
      <TextContainer>
        <h4>Boost your links today</h4>
        <Button> Get Started </Button>
      </TextContainer>
    </SectionContainer>
  );
};

export default BoostSection;
